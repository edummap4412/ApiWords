from sqlalchemy.orm import Session

"""def create_book(db: Session, book: BookSchema):
    vowel_counter = vowel_count(book.title)

    _book = Book(title=book.title, description=book.description)
    db.add(_book)
    db.commit()
    db.refresh(_book)
    return vowel_counter"""

